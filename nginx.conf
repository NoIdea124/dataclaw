server {
    listen 80;
    server_name your-server-ip-or-domain;  # ← 改成你的服务器 IP 或域名

    # 静态前端文件（部署目录）
    root /opt/dataclaw;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
